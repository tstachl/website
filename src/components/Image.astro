---
export interface Props {
  src: string;
  alt: string;
  className: string;
}

const { src, alt, className } = Astro.props;
---
<img class={className} data-src={`${Astro.site.origin}${src}`} alt={alt} />